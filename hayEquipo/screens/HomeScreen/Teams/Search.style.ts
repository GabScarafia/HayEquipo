import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    view:{
        height: "100%",
        width: '100%',
        paddingHorizontal:32,
        gap:8,
        paddingVertical:8,
    },
    title: {
        textAlign: 'center',
        fontWeight: 'bold',
    },
    
})
